謄本轉清冊｜免安裝桌面版

使用方式
1. 將下載的 ZIP 壓縮檔完整解壓縮到任一資料夾。
2. 開啟資料夾後，雙擊「謄本轉清冊.exe」。
3. 不需要安裝 Python，也不需要另外安裝 PaddleOCR。

注意
- 請勿直接在 ZIP 壓縮檔內執行程式，否則可能無法正常啟動。
- 第一次開啟時，程式會把內建的 OCR 模型準備到電腦中；視電腦效能可能需要一小段時間。完成後的每次啟動不會重新解壓整個程式。
- 文件與 OCR 結果都只在這台電腦中處理，不會上傳。
